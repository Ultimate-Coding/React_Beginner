import "./App.css";

function App() {
  return <></>;
}

export default App;

// In index.js file, you will see for initial render we call createRoot with the target DOM node,
// and then calling its render method with your component
